name: TapData Rollback

on:
  workflow_dispatch:
    inputs:
      target_env:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - dev
          - sit
          - UAT
          - PRE-PROD
          - PROD
      last_stable_tag:
        description: 'Tag to rollback to (required)'
        required: true
        type: string

jobs:
  rollback:
    # TODO: Replace {WORKER_REPO} with actual org/repo, e.g. your-org/tapdata-cicd-worker
    uses: "tap-jason/jason_test_cicd/.github/workflows/tapdata-rollback.yml@main"
    with:
      project: ${{ github.event.repository.name }}
      target_env: ${{ inputs.target_env }}
      last_stable_tag: ${{ inputs.last_stable_tag }}
      caller_repo: ${{ github.repository }}
      worker_repo: "tap-jason/jason_test_cicd"
    secrets: inherit
